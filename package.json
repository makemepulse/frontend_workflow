{
  "name": "workflow",
  "version": "1.0.0",
  "description": "",
  "main": "workflow/index.js",
  "scripts": {
    "stylus:compile": "node ./workflow stylus",
    "stylus:watch": "node ./workflow stylus -w",
    "stylus:build": "node ./workflow stylus -m",
    "browserify:compile": "node ./workflow browserify -s",
    "browserify:watch": "node ./workflow browserify -s -w",
    "browserify:build": "node ./workflow browserify -m",
    "watcher": "node ./workflow watch",
    "server": "node ./workflow server",
    "uglify": "node ./workflow uglify",
    "compile": "npm run stylus:compile & npm run browserify:compile",
    "watch": "npm run stylus:watch & npm run browserify:watch & npm run watcher",
    "build": "npm run stylus:build & npm run browserify:build && npm run uglify",
    "start": "npm run stylus:watch & npm run browserify:watch & npm run server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aliasify": "^1.8.0",
    "autoprefixer-stylus": "^0.8.1",
    "babel": "^5.8.23",
    "babelify": "^6.1.3",
    "babel-polyfill": "^6.3.14",
    "browser-sync": "^2.10.0",
    "browserify": "^12.0.1",
    "chokidar": "^1.3.0",
    "colors": "^1.1.2",
    "eslint": "^2.2.0",
    "fs-extra": "^0.26.2",
    "js-yaml": "^3.4.6",
    "mustache": "^2.2.0",
    "partialify": "^3.1.5",
    "rupture": "^0.6.1",
    "stylus": "^0.52.4",
    "subarg": "^1.0.0",
    "watchify": "^3.6.1"
  }
}
